extends layout

block content
    .container.page-contents
        if post
            h2= post.title
            p Author : 
                = post.author
            p.text-info= post.time
            p= post.post